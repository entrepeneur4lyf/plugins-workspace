interface:
  display_name: "Plugins Workspace"
  short_description: "Repo-specific patterns and workflows for plugins-workspace"
  default_prompt: "Use the plugins-workspace repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true